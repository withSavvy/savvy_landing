node_modules/
data/
.env
.DS_Store
*.log
backend/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Worktree-isolation scratch (new-session.sh) — never stage a checked-out worktree
.worktrees/

# Claude Code local settings (never commit — contains personal permission grants)
.claude/settings.local.json
